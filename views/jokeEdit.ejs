<!DOCTYPE html>
<head>
    <title>Dad Joke Wednesday</title>

    
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css?family=Roboto:300,300italic,700,700italic"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.css"
    />
    <link rel="stylesheet" href="../node_modules/milligram/dist/milligram.min.css" />
    <link rel="stylesheet" href="https://milligram.io/styles/main.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/milligram/1.4.1/milligram.css">
    <link rel="stylesheet" href = '../public/style.css' />
</head>

<body>
    <div > 
        <h1>&#128104 Dad Jokes</h1>
        <div>
            <ul>
                <% jokeList.forEach(details => { %>
                <% if (details._id == idJoke) { %>
                    <li>
                        <form action="/edit/<%= details._id %>" method="POST">
                            <input type="text" value="<%= details.content %>" name="content">
                            <button type="submit" class = 'button'>
                                Confirm
                            </button>
                            <button onclick = "window.location.href = '/'" class = 'button' class = 'cancel' >
                                Cancel
                            </button>
                            
                        </form>
                    </li>
                    <% } else { %>
                        <li>
                            <div>
                                <%= details.content %>
                            </div>
                            <button onclick = "window.location.href = '/edit/<%= details._id %>'" class = 'button button-small edit'>
                                Edit
                            </button>
                            <button onclick = "window.location.href = '/remove/<%= details._id %>'" class = 'button button-small edit'>
                                Delete
                            </button>
                                
                            </a>
                        </li>
                    <% } %>
                <% }) %>
            </ul>
        </div>
    </div>
</body>